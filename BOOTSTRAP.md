# Bootstrap

This workspace is already bootstrapped. See SOUL.md for Sentinel's identity, testing layers, and operating model. See AGENTS.md for the agent portfolio and spawn order. See the `github-pipeline` skill for canonical CI/CD workflow templates.

To bootstrap a **new target repo**, use `/scan <repo-path>` or `/sentinel-deploy <repo-path>`.
